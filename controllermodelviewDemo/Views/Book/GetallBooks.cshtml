@using controllermodelviewDemo.Models;
@model IEnumerable<BookModel>; 

<style>
    .container {
        margin-top: 80px;
    }
    .card {
        padding: 10px;

    }
</style>



<div class="container">
    <div class="row">

        @foreach (var data in Model)
        {
            @*<div class="card col-4 px-2">
                <img class="card-img-top" src="~/images/Mvcbook.jpg" alt="Card image cap">
                <div class="card-body">
                    <h5 class="card-title">@data.Title</h5>
                    <p class="card-text">@data.Description</p>

                </div>
                <div>
                    <a asp-controller="Book" asp-action="GetBook" asp-route-id="@data.id" class="btn btn-primary">Explore More...</a>
                </div>

            </div>*@

            <div class="card col-md-4 mt-6">
              
                    <svg class="bd-placeholder-img card-img-top" width="100%" height="180" xmlns="http://www.w3.org/2000/svg" aria-label="Placeholder: Image cap" preserveAspectRatio="xMidYMid slice" role="img"><title>Placeholder</title><rect width="100%" height="100%" fill="#868e96" /><text x="50%" y="50%" fill="#dee2e6" dy=".3em">Image cap</text></svg>

                    <div class="card-body">
                        <h5 class="card-title">@data.Title</h5>
                        <p class="card-text">@data.Description</p>

                    </div>
                    <div>
                        <a asp-controller="Book" asp-action="GetBook" asp-route-id="@data.id" class="btn btn-primary">Explore More...</a>
                    </div>
                
            </div>
        }
    </div>
</div>
<br />
<br />
<br />
<br />
<br />

